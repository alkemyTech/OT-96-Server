const activitiesRepository = require('../repositories/activities');

//example:
/**
 const getAll = async () => {
    return await activitiesRepository.getAll();
}

module.exports = {
    getAll
}
 */
